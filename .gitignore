# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Rust
target/

# Node / Vite — only relevant once ui/ lands (build-order Stage D)
node_modules/
dist/
*.log

# Local data — the SQLite event log is generated, never committed
*.sqlite
*.db

# Editors / OS
.vscode/
.idea/
.DS_Store
